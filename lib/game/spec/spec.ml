(** @canonical Cloakaml.Game.Spec *)

(** {1 ...} *)

module Term = Term
module EnumType = Enum_type
module EnumMap = Enum_map

(** {1 ...} *)

module Id = Id
(* module Role = Role *)
(* module Group = Group *)
module Roles = Roles
(* module Player = Player *)
module Players = Players
(* module Round = Round *)
module Rounds = Rounds
module Abilities = Abilities

module type S = sig

end

(* module type Roles = sig end *)
